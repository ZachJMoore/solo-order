{
  "scripts": {
    "migrate": "sequelize-cli db:migrate",
    "seed:all": "sequelize-cli db:seed:all",
    "dev": "nodemon -L index.js",
    "start": "node index.js",
    "startup:dev": "npm run migrate && npm run seed:all && npm run dev",
    "startup": "npm run migrate && npm run seed:all && npm run start"
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "nodemon": "^2.0.16",
    "pg": "^8.7.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.21.0",
    "sequelize-cli": "^6.4.1",
    "sqlite3": "^5.0.8"
  }
}
